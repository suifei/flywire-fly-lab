#!/usr/bin/env python3
"""docs/ecobox/AUDIT.md 由 results/eco/audit.json 渲染（数字不手抄）。"""
import json
from pathlib import Path
ROOT = Path(__file__).resolve().parent.parent; A = json.loads((ROOT / "results/eco/audit.json").read_text()); S = A["senses"]; sp = A["speed"]; F = A["flight"]; L = A["learning"]
WANT = [("食物", ["sugar", "odorA", "vinegar"], "尝得到（糖 → 伸喙）；闻得到但**不会朝它走**：气味进了脑子，到不了转向"),
        ("水", ["humid"], "湿度进了脑子，到不了任何运动读出；喝水靠的是踩到水之后的水味觉（属于糖味觉那组里的 17 个）"),
        ("地形高度", [], "**没有感受器通道**。坡度只通过身体起作用（上坡慢）。要让它「知道」坡度，只能作为本体感受直接给可塑性层，并标明不是连接组"),
        ("风", ["wind"], "触角 JO 被驱动，脑内有 300 多个神经元响应，但到不了转向 / 起飞；已知它通到梳理（灰尘）"),
        ("温度", ["heat"], "**接上了，而且能定向**：左热右热驱动的转向神经元相反——这颗连接组自带趋 / 避温"),
        ("危险", ["looming", "lc16", "sound"], "逼近 → 巨纤维起飞 + 定向转向；正面逼近 → 后退；声音单独不够起飞，但让逼近反应提前（§47）"),
        ("其他果蝇", ["cva", "sound"], "信息素 cVA 没接上（只有几十个神经元响应）；翼振声走听觉通路，同上；视觉上别的果蝇只会被当成逼近的物体")]
rows = []
for name, keys, note in WANT:
    v = " / ".join(f"{S[k]['label'].split('（')[0]}：{S[k]['verdict']}" for k in keys) or "没接上"
    rows.append(f"| {name} | {v} | {note} |")
detail = "\n".join(f"| {s['label']} | {s['n_left']} + {s['n_right']} | **{s['verdict']}** | {'能' if s['can_steer'] else '不能'}（{s['LI_left']:+.2f} / {s['LI_right']:+.2f}） | {'、'.join(s['triggers']) or '—'}{'；' + s['fixed_turn'] if s['fixed_turn'] else ''} | {s['both']['dnaL']:.0f} / {s['both']['dnaR']:.0f} | {s['both']['gf']:.0f} | {s['both']['mn9']:.0f} | {s['both']['odn1']:.0f} / {s['both']['bdn2']:.0f} | {s['both']['mbon']:.1f} | {s['both']['active']:.0f} |" for s in S.values())
yn = lambda b: "是" if b else "**否**"
md = f"""# 生态箱 M1 · 技术审计（由 `eco/render_audit.py` 从 `results/eco/audit.json` 渲染，不要手改数字）

> 规矩：后面的玩法**只许建立在「接上了」的项上**。「半接上」和「只到脑内」的项可以作为可塑性层的输入（那是后天学习要补的路），但不能当成果蝇天生会的事。

## 1. 你列的七种感知，现在各是什么状态

| 感知 | 实测判定 | 说明 |
|---|---|---|
{chr(10).join(rows)}

## 2. 每一路感受器的实测明细（{A['hz']} Hz，左 / 右 / 双侧各 {A['seeds']} 个种子；子回路 v5，{A['n_neurons']:,} 个神经元）

判定写在跑之前：「能定向」= 左右刺激的转向指数符号相反且 |LI| ≥ 0.2；「能触发」= 双侧驱动时 巨纤维 ≥ 90 / MN9 ≥ 10 / aDN1 ≥ 20 / MDN ≥ 20 Hz；都不满足但脑内活跃 ≥ 50 个 = 「只到脑内」。

| 感受器 | 神经元数 | 判定 | 能定向（左刺激 LI / 右刺激 LI） | 能触发 | 转向 DNa 左/右 | 巨纤维 | MN9 | 前进 oDN1 / BDN2 | MBON 均值 | 其他活跃神经元 |
|---|---|---|---|---|---|---|---|---|---|---|
{detail}

## 3. 七种动作

| 动作 | 由谁决定 | 状态 |
|---|---|---|
| 转向 | 连接组：转向神经元 DNa01/02 的左右差 → 左右步幅差 → 六条腿 | **接上了**（逼近、正面逼近、热能定向；气味 B、触碰只会朝固定一侧转；食物 / 水的气味不会） |
| 走 | 没有「开始走」的神经元接到身体上：行走是默认状态（这个模型没有自发活动）。前进神经元 oDN1 / BDN2 在回路里、会被热和气味 B 驱动，但**没有映射到步速** | **没接上**（走是默认的） |
| 停 | 没有「停」的神经元；只有进食 / 梳理时才停 | **没接上** |
| 进食 / 喝水 | 连接组：糖 / 水味觉 → MN9 伸喙；身体状态调感受器灵敏度（饿了更敏感） | **接上了**（但只在踩到食物 / 水之后） |
| 起飞 | 连接组：巨纤维 ≥ 90 Hz → 起飞（逃逸反射） | **接上了**（反射）；「主动起飞」没有神经通路 |
| 降落 | 手写：逃逸片段播完就落地 | **没接上** |

## 4. 飞行链路

- 起飞：{F['takeoff']}
- 空中：{F['in_flight']}（{F['n_clips']} 段录像：{'、'.join(f"{c['name']} {c['seconds']} s" for c in F['clips'])}）
- 降落：{F['landing']}
- **结论：{F['verdict']}。** 所以 M2 里「飞行作为可学技能」锁着；先天的逃逸起跳保留（它是连接组自带的反射，不是学来的）。M3 把「飞行倾向」（起飞阈值的高低 + 无威胁时主动起飞的概率）作为**可遗传**的量放开——飞一次是一段不可操控的弹道，耗能高、受风影响、落点随缘，值不值得飞由环境决定。

## 5. 学习链路

| | 实测 |
|---|---|
| 蘑菇体的气味记忆会形成吗 | {yn(L['mb_memory_forms'])} |
| 只记配对过的那一种吗 | {yn(L['mb_specific'])}（差一点，见 §48.3） |
| 记忆走得到运动输出吗 | {yn(L['memory_reaches_motor'])} |
| 糖 / 苦 / 热 / 湿能靠真实接线叫起多巴胺神经元吗 | {yn(L['dopamine_recruited_by_senses'])} |
| 页面引擎与 Python 学到的一致吗 | {yn(L['engine_parity'])} |

**结论：连接组自己不会因为「吃到东西」而改变行为。** 要让它在生存压力下学出策略，必须在连接组之外加一层可塑的读出（PLAN.md 里的「可塑性层」），奖励信号由身体的内部状态自动产生。连接组仍然是固定的身体：它提供先天反射，也提供可塑性层能读到的全部放电。

## 6. 速度：为什么需要响应面

| | 实测 |
|---|---|
| 真脑每 5 ms 物理步（安静 / 闻着气味） | {sp['ms_per_5ms_step_quiet']} ms / {sp['ms_per_5ms_step_smelling']} ms |
| 一只果蝇 | {sp['realtime_factor_one_fly']} × 实时 —— 开不了时间加速 |
| 32 只同时 | {sp['realtime_factor_32_flies']} × 实时 —— 不可能 |

所以快进和群体必须用**从真脑采样出来的响应面**（同一接口，输入频率 → 各读出群的发放率），镜头里那一只可以随时切回真脑并排核对；页面上写明谁在跑什么、响应面的拟合优度是多少。
"""
(ROOT / "docs/ecobox/AUDIT.md").write_text(md); print("→ docs/ecobox/AUDIT.md", len(md), "字")
